
export type RouteOptionDto = {
    optionIndex?: number,
    distanceMeters?: number,
    durationSeconds?: number,
    geometryType?: string,
    coordinates?: number[][],
    geometry?: {
        type?: string;
        coordinates?: number[][];
    },
    OptionIndex?: number,
    DistanceMeters?: number,
    DurationSeconds?: number,
    GeometryType?: string,
    Coordinates?: number[][],
    Geometry?: {
        Type?: string;
        Coordinates?: number[][];
    }
};
